# Publish project to npm

### Creating Libraries
``` ng generate application *projectName* ```

### Build project
``` ng build *projectName* --prod ```
 
### Publish to npm
```
cd dist/*projectName*
npm publish
```
